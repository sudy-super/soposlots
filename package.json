{
  "name": "soposlots",
  "version": "1.0.0",
  "description": "そぽたんスロット",
  "scripts": {
    "generate:ogp": "node generate-ogp.js",
    "build": "rm -rf public && mkdir public && cp index.html public/ && cp -r image public/",
    "deploy": "npm run build && wrangler pages deploy public --project-name=soposlots",
    "dev": "npm run build && wrangler pages dev public"
  },
  "dependencies": {
    "canvas": "^3.1.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
